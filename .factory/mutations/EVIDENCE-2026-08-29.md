# Mutation kill-rate 证据 — 2026-08-29（wop-typescript-sdk 移植重证）

- 命令：`python3 .factory/mutations/run.py`（全量，无 --only 过滤）
- 退出码：**0**（全部按预期；无 FAIL / SKIP / 配置错误；还原零残留——runner 内置逐文件字节校验通过）
- 缺陷集：14 条 = 篡改类 7（guard 门）+ 行为破坏类 5（tests 门）+ 负例 2（guard/tests 各 1）
- 来源：随黑盒工厂自 gtsp-wop-gateway 移植后**本仓重铸**（gateway 缺陷集锚定 awesome-rules
  路径，在本仓不可解析；重铸锚点全部指向本仓真实文件与测试）。

## 汇总（2026-08-29 实跑原文摘录）

```
===== mutation 冒烟汇总 =====
  [G-01] PASS       blocked=True（rc=1）符合预期
  [G-02] PASS       blocked=True（rc=1）符合预期
  [G-03] PASS       blocked=True（rc=2）符合预期
  [G-04] PASS       blocked=True（rc=1）符合预期
  [G-05] PASS       blocked=True（rc=1）符合预期
  [M-01] PASS       blocked=True（rc=2）符合预期
  [M-02] PASS       blocked=True（rc=2）符合预期
  [B-01] PASS       blocked=False（rc=0）符合预期
  [B-101] PASS      blocked=True（rc=1）符合预期
  [B-102] PASS      blocked=True（rc=1）符合预期
  [B-103] PASS      blocked=True（rc=1）符合预期
  [B-104] PASS      blocked=True（rc=1）符合预期
  [B-105] PASS      blocked=True（rc=1）符合预期
  [B-106] PASS      blocked=False（rc=0）符合预期
  篡改类拦截（guard 门）: 7/7 = 100%
  行为破坏类拦截（tests 门）: 5/5 = 100%
  正向缺陷拦截（kill rate）: 12/12 = 100%
  负例放行: 2/2
  结论: 门灵敏度冒烟通过（auto-merge 的必要非充分条件）
  周界指纹已绑定: c988e6cb51d0（evidence-stamp.json）
```

## 行为破坏类逐条击杀证据（注入 → 变红的用例，2026-08-29 实测）

| id | target | 注入 | kill 证据（注入后 vitest 实红用例） |
|---|---|---|---|
| B-101 | src/digest.ts | 大写 hex 分支短路 `if (/^sha-256 …[0-9A-F]+…/)` → `if (false && …)` | `digest.ts：格式钉五态文案全等（D2/I5） > 双空格/大写 hex/短 hex/未知标签（parse）/跨族（unsupported）全等` FAILED |
| B-102 | src/encode.ts | 尾随位严格校验短路 `if (lastIndex & …)` → `if (false)` | `base64url 编解码 > 严格模式：拒收非规范尾随位（RFC 4648 §3.5）`、`encode.ts：六条错误文案全等（F7/D1） > base64url 严格三态文案全等`、`formatRules 全量消费 > 循环全量` FAILED（3 用例） |
| B-103 | src/keys.ts | DER 最小长度守卫放水 `< 40` → `< 0` | `keys 边界 > 合法 base64 但 DER 解码后过短 → 拒绝`、`keys.ts：材料解析边界与文案全等（D12） > 40 字符边界` FAILED |
| B-104 | src/canonical.ts | Java-URLEncoder 补编码回调 `(c) => '%' + …` → `(c) => c` | `javaUrlEncode（Java URLEncoder 语义，F2） > …!'()~ 补编码为大写 %XX` FAILED |
| B-105 | src/envelope.ts | DEK 三段结构校验失效 → `if (false)` | `envelope.ts：DEK 载荷段结构文案全等（§6.1） > 两段/空第三段 → 三段格式文案` FAILED |
| B-106（负例） | src/digest.ts | 文件头注释措辞调整（行为等价） | 175 用例 + 15 BDD 场景仍绿，门放行 rc=0（防门全拒） |

每条 find 均经预验证「恰好命中一次 + 注入后对应套件真红 + 字节还原一致」后才入缺陷集。

## 口径说明

- tests 门命令：`scripts/run_tests.sh --no-lock`（ADR-009 数据化，factory-local.json
  final_gate_cmd）= `npm run test`（vitest 全量，13 文件 175 用例）+ `npm run test:bdd`
  （cucumber 商户旅程，15 场景 49 步）；`--no-lock` 为工厂链约定旗标，本仓无锁，消费并忽略。
- 退出码语义：rc=1 = 击杀证据；rc=0 = 放行；域外退出码/超时 = 无效运行（judge 判 FAIL）。
  guard 门 rc=2 为 fail-closed 设计（=拦截）。
- 门超时预算：guard 300s / tests 600s（tests 门实测基线 ~2.6s，≈230 倍余量）。
- 行为类 kill rate 100%（≥80% 为下限；低于 100% 即缺陷选材或门有洞）。
- 周界指纹绑定：evidence-stamp.json 的 perimeter_blob = c988e6cb51d0…（factory-local.json
  的 git blob hash，机器生成）；改周界后不重跑本 runner，stamp 即宣告过期。
- run.py 的 write_stamp 默认证据指针已对准本文件（mutations/ 属 DISTRIBUTION skip 类，
  仓内资产；上游默认值为 gateway 移植时点日期）。
- 本证据为「必要非充分」条件；auto-merge 默认关闭（铁律 5），开启属治理动作，由人类决定。
