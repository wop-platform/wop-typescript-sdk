# language: zh-CN
功能: 商户接入 WOP 网关全旅程
  商户从拿到 appKey 与密钥对，到安全收发 L0/L2 报文、防御篡改与失控响应的完整使用场景。
  规格依据: wop-sdk-spec v1.0-ratified F1-F9 / 概念 API §2 / 附录 D（用例矩阵见 tests/scenario-matrix.md）

  背景:
    假如 一套 RSA3072 测试密钥与 appKey "bdd-app-key"

  # S1-S3 初始化与套件配置（F1）
  场景: 商户以合法配置初始化客户端
    当 商户用 "WOP-RSA3072-SHA256" 套件初始化客户端
    那么 客户端构造成功

  场景: 商户误配国密套件得到明确的暂未支持
    当 商户用 "WOP-SM2-SM3" 套件初始化客户端
    那么 初始化失败并提示 "SM2-SM3 套件暂未支持"

  场景: 商户误配跨族套件被明确拒绝
    当 商户用 "WOP-RSA3072-SM3" 套件初始化客户端
    那么 初始化失败并提示 "跨族"

  # S4-S6 出向请求（F2-F5/F9）
  场景: 商户发起 L0 订单创建请求且网关可验签
    当 商户对订单接口发起 L0 请求
    那么 网关侧公钥验签通过
    并且 线上体为原文

  场景: 商户发起无 body 查询请求时摘要头缺席
    当 商户对查询接口发起无 body 的 GET 请求
    那么 摘要头缺席且不入签

  场景: 商户发起 L2 敏感数据请求且网关可解密回文
    当 商户对订单接口发起 L2 加密请求
    那么 线上体为单字段密文载体且网关解密回文为原文

  # S7-S11 入向校验（F6/I2/I3/I7）
  场景: 商户校验 L0 同步响应
    当 商户校验平台的 "L0" 同步响应
    那么 校验通过且明文为原文

  场景: 商户校验 L2 同步响应得到明文
    当 商户校验平台的 "L2" 同步响应
    那么 校验通过且明文为原文

  场景: 响应签名被篡改时得到模糊失败
    当 商户校验被篡改了 "signature" 的 L0 响应
    那么 校验失败且原因为 "签名验证失败"
    并且 失败原因不泄露验签或解密细节

  场景: 响应 body 被篡改时得到明确的摘要不匹配
    当 商户校验被篡改了 "digest" 的 L0 响应
    那么 校验失败且原因包含 "摘要不匹配"

  场景: L2 响应 DEK 被篡改时得到模糊解密失败
    当 商户校验被篡改了 DEK 载荷的 L2 响应
    那么 校验失败且原因为 "解密失败"
    并且 失败原因不泄露验签或解密细节

  # S10 异步回调（F6）
  场景: 商户以完整 URL 校验异步回调
    当 商户校验平台对回调地址 "https://merchant.example.cn/callback/order?src=async" 的通知
    那么 校验通过且明文为原文

  场景: 回调 path 与签名不符时校验失败
    当 商户校验平台签名到另一路径的回调
    那么 校验失败且原因为 "签名验证失败"

  # S11 重放确定性（§2）
  场景: 同参数重放构造请求得到全等结果
    当 商户对订单接口发起 L0 请求
    并且 商户以同参数再构造一次 L0 请求
    那么 两次请求完全一致

  # S12 失控响应防护（D4）
  场景: 响应体超过 11MiB 上限时断流拒绝
    当 平台返回超过 11MiB 上限的无限响应体
    那么 响应体读取被流式拒绝
